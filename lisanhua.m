%离散化

% H = tf([0.25],[1 50 0]);
% Hd = c2d(H,1,'z')
% 
% num=[0.25];den=[1 50 0];
% [A,B,C,D]=tf2ss(num,den); 
% 
% A=[0 1;0 -50];
% B=[0;0.25];
% C=[1 0];
% D=0;
% [num,den]=ss2tf(A,B,C,D);

% t=0:0.002:8; %时间采样
% u=sin(pi*t); %输入信号
% y=cos(pi*t); %输出信号
% xx=[-y(2:1000),-y(1:999),u(2:1000),u(1:999)]; %定义tH
% x=xx';
% inv(x'.*x).*x'.*(y(6:4001)') %得到最小二乘法辨识结果

% x=1790:10:2010;
% y=[3.9,5.3,7.2,9.6,12.9,17.1,23.2,31.4,38.6, 50.2,63.0,76.0,92.0,105.7,122.8,131.7,150.7,179.3,203.2,226.5,248.7,281.4,308.7];
% plot(x,y,'*');
% p=polyfit(x,y,3);
% polyval(p,2020)
% plot(x,y,'*',x,polyval(p,x));


H = tf([0.013 -0.00012],[1 0.014 0.002])
Hd = c2d(H,1,'z')


